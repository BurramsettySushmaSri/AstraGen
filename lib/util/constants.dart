import 'package:flutter/material.dart';

class constants {
  static double sizeboxheight = 100.0;
  static const int namelen = 3;
  static const double form_pa_left = 850.0;
  static const double form_pa_top = 40.0;
  static const pad = EdgeInsets.all(8.0);
  static const paddings= EdgeInsets.all(20);
}
